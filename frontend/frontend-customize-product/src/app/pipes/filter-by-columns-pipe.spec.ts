import { FilterByColumnsPipe } from './filter-by-columns-pipe';

describe('FilterByColumnsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByColumnsPipe();
    expect(pipe).toBeTruthy();
  });
});
