import { FilterCustomizationsPipe } from './filter-customizations-pipe';

describe('FilterCustomizationsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCustomizationsPipe();
    expect(pipe).toBeTruthy();
  });
});
